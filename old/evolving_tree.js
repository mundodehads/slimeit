/*
First object key is equal a race parent node,
  inside that key object the keys represents it children nodes.

The children nodes value represents it nutrient threshold to evolve,
  it should be arranged from largest to smallest number.
*/

module.exports = {
    normal: {
        earth: 100,
        grass: 100,
    },
};
